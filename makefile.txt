$<:表示所有的依赖目标集（也就是foo.c）
$@:表示所有目标集（也就是foo.o）
?=:如果没有被定义则被赋值，否则保持原值
:=:不支持延迟定义
$(var:a = b),:将变量ｖａｒ中所有ａ后缀替换成ｂ后缀
$(var:%.o=%.c):将变量ｖａｒ中符合前一个模式的替换成后一个模式
$(subst,a,b,str):将变量str中的ａ后缀替换成ｂ后缀
$(wildcard *.c):展开变量
只有以ｔａｂ键作为开始，才被认为是命令

